extends layout

block content
  h1= title
  p Welcome to #{title}

res.render('index', { title: "Mini Messageboard", messages: messages }).

  if (user == "Amando") {
    document.h1.textContent = user;
    document.p.textContent = text + " " + date;
  }
