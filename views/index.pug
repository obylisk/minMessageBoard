extends layout

block content
  h1= title
  p Welcome to Obylisk's #{title}
  ul
    each val in messages
      li #{val.user}: #{val.text}
      | #{val.added}      
      
  a(href="/new") New Message
